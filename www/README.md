# burn_automata web

Static documentation and the browser viewer publishing surface live here.

The current page links to the local Rust workflow and reserves `pkg/` for wasm artifacts produced by `scripts/build_wasm.sh`.
