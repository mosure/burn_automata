# Local Sandbox

This directory is intentionally empty in git. Put one-off experiments, sweeps,
resume recipes, and failed diagnostics here; every other file under this
directory is ignored.

Promote a recipe to [`../verified/`](../verified/) only after removing
machine-specific paths, adding explicit gates, and documenting its status.
